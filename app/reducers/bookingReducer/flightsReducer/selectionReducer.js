import {

} from '../../../actions/types';

const INITIAL_STATE = {
  depart1: null,
  destination1: null,
  depart2: null,
  destination2: null
};

export default (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
