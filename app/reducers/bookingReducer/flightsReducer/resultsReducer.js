import {

} from '../../../actions/types';

const INITIAL_STATE = {
  allResults: {},
  filteredByOneStop: {},
  filteredByTwoStops: {},
};

export default (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
