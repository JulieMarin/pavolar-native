const recommendationsPackager = (recommendations) => {
  // recommendations.forEach((recommendation) => {
  //   recommendation.combinations.forEach((combination) => {
  //     console.log(combination.flightChoices)
  //   })
  // })
};

export { recommendationsPackager }
